node_modules
.next
out
# Holds secrets and connection strings — copy .env.example to .env locally.
# .env.example is the only one committed.
.env*
!.env.example
next-env.d.ts
*.tsbuildinfo
.DS_Store
.vercel

# Machine-local Docker port overrides. The documented setup is in
# docker-compose.yml; override locally when another project holds the port.
docker-compose.override.yml

# `vercel env pull` output — carries real project keys.
venv.txt
